class Solution {
public:
    int countSeniors(vector<string>& details) {
        int cnt=0;
        for(auto it: details){
            int num=(it[11]-'0')*10+(it[12]-'0');
            cout<<num<<endl;
            if(num>60){
                cnt++;
            }
        }
        return cnt;
    }
};
