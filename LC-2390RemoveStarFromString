class Solution {
public:
    string removeStars(string s) {
        string newStr="";
        for(int i=0;i<s.length();i++){
            if(s[i]=='*'){
               newStr.pop_back();
            }
            else
            newStr+=s[i];
        }
        return newStr;
    }
 };
