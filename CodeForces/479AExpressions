#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    int a1=max(a+b+c,a*b*c);
    int a2=max(a*(b+c),(a+b)*c);
    int a3=max(a1,a2);
        int res=max(a3,max((a*b)+c,a+(b*c)));
        cout<<res<<endl;

    return 0;
}
